name: Build WorldGate

on:
  push:
    branches: [ main, worldgate-essential-style-ui ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.7.1'

      - name: Verify toolchain
        run: |
          java -version
          gradle --version

      - name: Build all versions
        run: gradle build --stacktrace --no-daemon

      - name: Verify built JARs
        run: |
          test -n "$(find v1_21_1/build/libs -maxdepth 1 -type f -name '*.jar' -print -quit)"
          test -n "$(find v26_1_2/build/libs -maxdepth 1 -type f -name '*.jar' -print -quit)"
          echo "1.21.1 artifacts:"
          find v1_21_1/build/libs -maxdepth 1 -type f -name '*.jar' -print
          echo "26.1.2 artifacts:"
          find v26_1_2/build/libs -maxdepth 1 -type f -name '*.jar' -print

      - name: Upload 1.21.1 jar
        uses: actions/upload-artifact@v4
        with:
          name: WorldGate-1.21.1
          path: v1_21_1/build/libs/*.jar
          if-no-files-found: error

      - name: Upload 26.1.2 jar
        uses: actions/upload-artifact@v4
        with:
          name: WorldGate-26.1.2
          path: v26_1_2/build/libs/*.jar
          if-no-files-found: error
